# 🧩 Proyecto Backend I — Entrega N°2

### 💬 Descripción general
Proyecto desarrollado en Node.js utilizando **Express**, **Handlebars** y **Socket.io**, correspondiente a la segunda entrega del curso **Backend I (CoderHouse)**.  
El objetivo fue implementar un sistema de productos con actualización en tiempo real mediante WebSockets.

---

## 🚀 Funcionalidades principales

### 🟢 Servidor Express
- Configurado en `src/app.js` con estructura modular.
- Endpoints base:
  - `/` → vista principal con pestañas (Productos / Tiempo Real)
  - `/realtimeproducts` → vista con conexión a WebSockets
  - `/api/products` → listado HTTP de productos (JSON)
  - `/api/carts` → rutas de carritos (sin cambios desde la entrega 1)

### 🟠 Handlebars (motor de plantillas)
- Configurado con layout principal `views/layouts/main.handlebars`.
- Vistas:
  - `home.handlebars`: vista principal con pestañas y fetch HTTP.
  - `realtimeproducts.handlebars`: interfaz en tiempo real con formularios para crear y eliminar productos.

### 🔵 WebSockets (Socket.io)
- Configuración en `index.js` con eventos:
  - `productosActuales` → envía la lista inicial de productos.
  - `crearProducto` → agrega un nuevo producto al archivo `products.json`.
  - `eliminarProducto` → elimina un producto existente por ID.
  - Ambos actualizan automáticamente la lista en todos los clientes conectados.

### 🟣 ProductManager
- Clase ubicada en `src/managers/ProductManager.js`.
- Maneja lectura y escritura del archivo `src/data/products.json`.
- Métodos implementados:
  - `getProducts()`
  - `addProduct(producto)`

---

## 📂 Estructura del proyecto...

